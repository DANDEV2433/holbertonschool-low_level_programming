#include "main.h"
/**
* _atoi - check the code
* @s:
* description: Write a function that convert a string to an integer
* Return: (0)
*/
int _atoi(char *s)
{
	int i = 0;

	while (s[i] != '\0')
	{
	if (s <= 98)
	{
	return (s);
}
