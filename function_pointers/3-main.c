#include <stdio.h>
#include "3-calc.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	num1 = atoi(a[i]);
	num2 = atoi(b[i]);
	calc = num1 operator num2;

    struct struct op_t
	if ( a[i] || b[i] == calc)
	return (calc);
    return (0);
}
